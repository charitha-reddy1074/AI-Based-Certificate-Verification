{
  "name": "certchain",
  "version": "1.0.0",
  "description": "Blockchain-based certificate verification system with biometric authentication",
  "main": "CertChain-main/dist/index.cjs",
  "scripts": {
    "dev": "cd CertChain-main && npm run dev",
    "build": "cd CertChain-main && npm run build",
    "start": "cd CertChain-main && npm start",
    "compile": "cd blockchain && hardhat compile",
    "test": "cd blockchain && hardhat test",
    "deploy": "cd blockchain && hardhat run scripts/deploy.js"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^6.1.0",
    "dotenv": "^17.2.3",
    "drizzle-kit": "^0.31.8",
    "hardhat": "^2.28.3"
  },
  "dependencies": {
    "axios": "^1.13.2",
    "ethers": "^6.16.0",
    "express": "^4.18.2",
    "mongoose": "^9.1.5",
    "dotenv": "^17.2.3",
    "zod": "^4.3.5"
  },
  "type": "module"
}
